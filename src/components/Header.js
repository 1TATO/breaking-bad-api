import React from 'react';
import logoImg from '../img/logo.png'

const Header = () => {
  return (
    <header className='center'>
      <img src={logoImg} alt='' />
    </header>
  )
};

export default Header;
